import { ReactElement } from 'react';

import * as styles from './Header.css';

const Header = (): ReactElement => {
  return <header className={styles.header}>SoundUX</header>;
};

export default Header;
